package com.resolum.intiva.core.ui.snackbar

/** Defines the types of snackbars that can be displayed, such as success, error, or normal. */
enum class SnackBarType {
    Success,
    Error,
    Normal
}
